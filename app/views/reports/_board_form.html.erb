<div class="report-form-container">
  <%= form_with model: report, url: create_board_report_report_path(board_id: board.id) do |f| %>
    <%= render "shared/error_messages", object: f.object %>
    <div class="form-group">
      <%= f.text_area :body, class: "form-control", placeholder: "報告理由を記載", onkeyup: "ReportShowLength(value);" %>
      <p id="inputlength">0文字/500文字</p>
    </div>
    <%= f.hidden_field :board_id, value: board.id %>
    <div class="buttons">
      <%= f.submit "送信", class: "form-control btn btn-outline-primary", id: "report-submit-button", disabled: true %>
      <%= link_to "キャンセル", board_path(board), class: "btn btn-outline-danger cancel-button" %>
    </div>
  <% end %>
</div>
