<%= render @board %>
<div class="board-container">
  <div class="create-board">
    <% if current_user %>
      <%= link_to new_board_path(parent_board: @board.id), class: "#{"func-icon" unless current_user}" do %>
        <i class="bi bi-plus-square" id="create-new-board"></i>
      <% end %>
    <% else %>
      <div class= "<%= "func-icon" unless current_user %>">
        <i class="bi bi-plus-square"></i>
      </div>
    <% end %>
  </div>
  <% @boards.each do |board| %>
    <div class="board-thumbnail-wrapper" id="board-thumbnail-wrapper-<%= @boards.index(board)+1 %>">
      <%= link_to board_path(board) do %>
        <div class="board-thumbnail-container" id="board-thumbnail-container-<%= @boards.index(board)+1%>">
          <div class="board-thumbnail">
            <% if !board.frames.empty? %>
              <% if board_image(board) %>
                <%= image_tag board_image(board).image %>
              <% else %>
                <% first_frame = board.frames.order(:frame_number).first %>
                <div class="board-thumbnail-text"><%= simple_format(first_frame.body) %></div>
              <% end %>
            <% else %>
              <p>コンテンツがありません</p>
            <% end %>
          </div>
        </div>
        <div class="board-content <%= board_color(board) %>" id="board-content-<%= @boards.index(board)+1%>">
          <%= board.title %>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
<%= render "shared/breadcrumb", breadcrumb: @breadcrumb %>

<style>
<%# 全体のボーダーを表示・スクロールバーを非表示%>
* {
  /*outline: 2px solid blue;*/
  scrollbar-width: none;
}
</style>

 
<style>
@media (max-width: 599px){
  .wrapper {
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 1500px;
  }
  .comment-area-wrapper {
    width: 100%;
    margin-top: 10px;
  }
  .board-body-text {
    font-size: 20px;
  }
  .comment-form-wrapper {
    min-width: 300px;
    width: 90%;
  }
   .board-title-label {
    font-size: 24px;
  }
  .board-title-label {
  flex-direction: column;
  }
  .search-form-container {
    width: 100%;
    white-space: nowrap;
  }
  .search-form-container input[type="search"] {
    width: 80%;
  }
  .mypage-container {
    display: none;
  }
  .mypage-container {
    position: relative;
    flex: 1;
    display: flex;
    justify-content: flex-end;
    top: -50%;
    display: none;
  }
}
@media (min-width: 600px) and (max-width: 1000px) {
  .wrapper {
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 1500px;
  }
  .comment-area-wrapper {
    width: 100%;
    margin-top: 10px;
  }
  .board-body-text {
    font-size: 20px;
  }
  .comment-form-wrapper {
    width: 500px;
  }
  .board-title-label {
    font-size: 30px;
  }
  
}
@media (min-width: 1001px) {
  .wrapper {
    height: 80vh;
  }
  .scroll-content .container {
    position: absolute;
    top:10%;
  }
  .board-body-text {
    font-size: 24px;
  }
  .comment-area-wrapper {
    margin-left: 10px;
  }
  .comment-form-wrapper {
    width: 50%;
  }
  .board-title-label {
    font-size: 30px;
  }
}
</style>

<style>
  <%# // ボード　スクロール部分 %>
  .scroll-content {
    border-radius: 5px;
    position: relative;
    flex: 5; 
    width: 100%;
    border: 1px solid;
    align-items: center;
    display: flex;
    background-color: white;
    overflow: hidden;
  }
  .container {
    overflow: auto;
    scroll-snap-type: y mandatory;
    height: 80%;
    width: 100%;
    margin: auto;
    z-index: 1;
  }
  .section {
    scroll-snap-align: start;
    height: 100%;
    border: 2px solid #ddd;
    overflow: hidden;
    display: flex;
    justify-content: center;
    align-items: center;
    margin-bottom: 100px;
    position: relative;
  }
  .frame-number {
    font-size: 20px;
    position: absolute;
    top: 10px;
    right: 10px;
    background: #ddd;
    height: 30px;
    padding: 2px 5px;
    text-align: center;
    border-radius: 50%;
    opacity: 0.6;
  }
  .board-body {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: left;
    width: 100%;
    height: 100%;
  }
  .board-body-text {
    text-align: left;
    padding: 0 10px;
    overflow: auto;
  }
  .board-body-text p {
    word-break: break-word;
    overflow-wrap: break-word;
  }
  .board-body img {
    max-width: 100%;
    max-height: 100%;
    object-fit: contain;
    margin: auto;
    display: block;
  }
  .create-board {
    width: 100px;
    text-align: center;
  }
  .create-board i {
    font-size: 50px;
  }
  
  .user-name {
    display: inline-block;
    margin: auto;
  }
  .board-image img {  <%# これらのプロパティはimgに直接指定することに注意 %>
    max-width: 100%; <%# 100％で表示できるのは元の大きさまで %><%# クリックで100%に変更　＝＞　全体表示 %>
    max-height: 100%; <%# 小さい画像をあえてはみ出させたい場合は直接指定するしかない、比率を維持したまま親要素に合わせて自動でサイズを調整することができなくなる %>
    min-width: 100px;
    min-height: 100px;
    object-fit: cover;
    border-radius: 15px 15px 15px 15px;
  }
</style>
<style>
.comment-area-wrapper {
  flex: 3;
  display: flex;
  flex-direction: column;
  height: 100%;
  border: 1px solid;
  position: relative;
  border-radius: 5px;
}
.comment-area {
  position: relative;
  height: 600px;
  overflow: auto;
  background-color: #fff;
  border: 5px solid white;
  border-radius: 5px;
}
.comment-area-label {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: 50px;
  font-size: 30px;
  font-weight: bold;
  border-bottom: 2px solid black;
  border-radius: 5px 5px 0 0;
  position: relative;
  background: #333;
}
.comment-area-label a {
  text-decoration: none;
  color: #fff;
}
.comment-area-label a::after {
	content: '';
	position: absolute;
	bottom: 0;
	left: 0;
	width: 100%;
	height: 3px;
	background-color: #fff;
	transform: scaleX(0);
	transition: transform 0.3s;
}

.comment-area-label a:hover::after {
	transform: scaleX(1);
}
.comment-track {
  position: absolute;
  width: 100%;
  text-align: left;
  animation: slideDown var(--animation-duration) linear infinite;
}

.comment-image img {
  width: 100px;
}
.comment-link {
  text-decoration: none;
}
.comment-link:hover {
  font-weight: bold;
}
.comment_by_current_user.comment_by_board_user {
  border-bottom: 2px solid green;
}
.comment_by_current_user {
  border-bottom: 2px solid green;;
}
.comment_by_board_user {
  border-bottom: 2px solid red;
}

@keyframes slideDown {
  from {
    transform: translateY(var(--start-translate, -105%));
  }
  to {
    transform: translateY(var(--end-translate, -200px));
  }
}
</style>
<!-- ボードタイトル !-->
<style>
  .board-title-label {
    width: 100%;
    display: flex;
    justify-content: space-between;
    overflow: hidden;
    position: absolute;
    top: 0;
    font-weight: bold;
    background: linear-gradient(90deg, rgba(255,112,30,1) 0%, rgba(255,191,48,1) 50%, rgba(255,77,48,1) 100%);
    background-size: 200% 100%;
    border: none;
    transition: .4s cubic-bezier(0.37, 0, 0.63, 1);
    box-shadow: inset 0 -5px 8px rgba(0, 0, 0, 0.1);
    color: #fff;
    border-radius: 5px 5px 0px 0px;
    margin: 0;
    padding: 0px 20px;
    overflow: auto;
    white-space: nowrap;
    border-bottom: 2px solid black;
  }
  .delete-edit-button {
    font-size: 30px;
    z-index: 99;
  }
  .delete-edit-button.funcs div{
    display: inline;
    margin: auto;
  }
  .delete-edit-button i {
    color: #333;
    margin: auto;
  }
  .delete-edit-button a {
    text-decoration: none;
    color: black;
    margin: auto;
  }
  .delete-edit-button i:hover  {
    color: #fff;
  }
</style>
<style>
 <%# 閲覧者数 %>
  .visitor-like-container {
    position: absolute;
    left: 50%;
    bottom: 10px;
    transform: translate(-50%, 0);
    z-index: 100;
    display: flex;
    gap: 20px;
  }
  .visitor-count {
    font-weight: bold;
    text-align: center;
    border: none;
    box-shadow: inset 0 -5px 8px rgba(0, 0, 0, 0.3);
    border-radius: 8px;
    padding: 5px 10px;
  }
  .visitor-count i {
    font-size: 20px;
  }
  <%# お気に入りボタン %>
  .like-button {
    background: #ff701e;
    border: 2px solid #ff701e;
    border-radius: 8px;
    color: #fff;
    font-weight: bold;
    background: linear-gradient(90deg, rgba(255,112,30,1) 0%, rgba(255,191,48,1) 50%, rgba(255,77,48,1) 100%);
    background-size: 200% 100%;
    border: none;
    transition: .4s cubic-bezier(0.37, 0, 0.63, 1);
    box-shadow: inset 0 -5px 8px rgba(0, 0, 0, 0.1);
    padding: 5px 10px;
    min-width: 180px;
   }
   .like-button:hover {
    background: #fff;
    letter-spacing: 0.15em;
    border: 2px solid #ff701e;
   }
   .like-button .unlike span, .like-button .like span {
    color: #fff;
    font-weight: bold;
   }
   .like-button:hover span {
    color: #ff701e;
   }
   .like-button a {
    font-size: 20px;
    text-decoration: none;
   }
   .like.not-login span {
    padding: 5px 10px;
    font-size: 20px;
    text-decoration: none;
    cursor: pointer;
   }
</style>

<!-- タグ -->
<style>
.board-container {
  width: 100%;
  min-width: 300px;
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  align-items: center;
  gap: 10px;
  padding: 10px;
  width: 100%;
  box-sizing: border-box;
}
.create-board i {
  color: #ff701e;
  transition: .7s;
}
.create-board i:hover {
  color: gold;
  letter-spacing: 0.2em;
}
.board-thumbnail-wrapper {
  display: flex;
  flex-direction: column;
  position: relative;
}
.board-thumbnail-container {
  position: absolute;
  top: -300px;
  overflow: hidden;
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  height: 300px;
  background: white;
  border: 2px solid #ddd;
  border-radius: 20px;
  display: none;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.5);
  z-index: 100;
}
.board-thumbnail {
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  height: 100%;
}
.board-thumbnail-text {
  text-align: center;
  padding: 5px;
}
.board-thumbnail img {
  max-width: 100%;
  max-height: 100%;
  object-fit: contain;
  margin: auto;
  display: block;
}

.board-container .board-content {
  border: 2px solid #ddd; 
  border-radius: 10px;
  padding: 10px 20px;
  text-align: center;
  width: 100%;
  box-sizing: border-box;
  font-size: 20px;
  font-weight: bold;
}
.board-container .board-content.red, .title-board-content.red {
  background: #F4ADB0;
}
.board-container .board-content.green, .title-board-content.green {
  background: #C9E2C7;
}
.board-container .board-content.blue, .title-board-content.blue {
  background: #ABECF2;
}
.board-container .board-content.brown, .title-board-content.brown {
  background: #EFDDAA;
}
.board-container .board-content.emerald, .title-board-content.emerald {
  background: #E4EDAA;
}

.board-thumbnail-wrapper a { 
  color: black;
  text-decoration: none;
}
</style>

<!-- チャット入力フォーム -->
<style>
  .comment-form-wrapper {
    position: fixed;
    top: 70%;
    left: 50%;
    transform: translate(-50%, -50%);
    height: 300px;
    border-radius: 20px;
    z-index: 999;
    display: none;
  }
  .chat-form textarea {
    position: absolute;
    height: 100%;
    padding-bottom: 60px;
    bottom: 0;
    resize: none;
    background-color: white;
    font-size: 20px;
    border-radius: 5px;
    box-shadow: inset 0 -5px 8px rgba(0, 0, 0, 0.1);
  }
  .chat-form input {
    width: 100%;
    opacity: 90%;
    height: 50px;
    font-size: 20px;
    border-radius: 0px 0px 5px 5px;
    background: #ff701e;
    border: none;
    font-weight: bold;
    background: linear-gradient(90deg, rgba(255,112,30,1) 0%, rgba(255,191,48,1) 50%, rgba(255,77,48,1) 100%);
    background-size: 200% 100%;
    transition: .8s cubic-bezier(0.37, 0, 0.63, 1);
  }
  .chat-form input[type="submit"]:hover {
    background: #fff;
    color: #ff701e;
    letter-spacing: 0.15em;
  }
  .chat-form input[type="submit"] {
    color: #fff;
    position: absolute;
    bottom: 0;
  }
  .chat-form input[type="submit"]:hover {
    opacity: 0.6;
  }
</style>