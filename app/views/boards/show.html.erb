<%= render @board %>
<div class="board-container">
  <div class="create-board">
    <% if current_user %>
      <%= link_to new_board_path(parent_board: @board.id), class: "#{"func-icon" unless current_user}" do %>
        <i class="bi bi-plus-square"></i>
      <% end %>
    <% else %>
      <div class= "<%= "func-icon" unless current_user %>">
        <i class="bi bi-plus-square"></i>
      </div>
    <% end %>
  </div>
  <% @boards.each do |board| %>
    <div class="board-thumbnail-wrapper" id="board-thumbnail-wrapper-<%= @boards.index(board)+1 %>">
      <%= link_to board_path(board) do %>
        <div class="board-thumbnail-container" id="board-thumbnail-container-<%= @boards.index(board)+1%>">
          <div class="board-thumbnail">
            <% if !board.frames.empty? %>
              <% first_frame = board.frames.order(:frame_number).first %>
              <% if first_frame.image.attached? %>
                <%= image_tag first_frame.image %>
              <% else %>
                <div class="board-thumbnail-text"><%= simple_format(first_frame.body) %></div>
              <% end %>
            <% else %>
              <p>まだコンテンツがありません</p>
            <% end %>
          </div>
        </div>
        <div class="board-content <%= board_color(board) %>" id="board-content-<%= @boards.index(board)+1%>">
          <%= board.title %>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
<%= render "shared/breadcrumb", breadcrumb: @breadcrumb %>

<style>
<%# 全体のボーダーを表示・スクロールバーを非表示%>
* {
  /*outline: 2px solid blue;*/
  scrollbar-width: none;
}
</style>

 
<style>
@media (max-width: 599px){
  .wrapper {
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 1500px;
  }
  .comment-area-wrapper {
    width: 100%;
  }
  .board-body-text {
    font-size: 20px;
  }
  .comment-form-wrapper {
    min-width: 300px;
    width: 90%;
  }
   .board-title-label {
    font-size: 24px;
  }
  .board-title-label {
  flex-direction: column;
  }
  .search-form-container {
    width: 100%;
    white-space: nowrap;
  }
  .search-form-container input[type="search"] {
    width: 80%;
  }
  .mypage-container {
    display: none;
  }
  .mypage-container {
    position: relative;
    flex: 1;
    display: flex;
    justify-content: flex-end;
    top: -50%;
    display: none;
  }
}
@media (min-width: 600px) and (max-width: 1000px) {
  .wrapper {
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 1500px;
  }
  .comment-area-wrapper {
    width: 100%;
  }
  .board-body-text {
    font-size: 20px;
  }
  .comment-form-wrapper {
    width: 500px;
  }
  .board-title-label {
    font-size: 30px;
  }
  
}
@media (min-width: 1001px) {
  .wrapper {
    height: 80vh;
  }
  .scroll-content .container {
    position: absolute;
    top:10%;
  }
  .board-body-text {
    font-size: 24px;
  }
  .comment-form-wrapper {
    width: 50%;
  }
  .board-title-label {
    font-size: 30px;
  }
}
</style>

<!-- ボードタイトル !-->
<style>
  .board-title-label {
    width: 100%;
    display: flex;
    justify-content: space-between;
    overflow: hidden;
    position: absolute;
    top: 0;
    font-weight: bold;
    background-color: #C9E2C7;
    border: 2px solid black;
    border-radius: 20px 20px 0px 0px;
    margin: 0;
    padding: 0px 20px;
    overflow: auto;
    white-space: nowrap;
  }
  .delete-edit-button {
    font-size: 30px;
    z-index: 99;
  }
  .delete-edit-button.funcs div{
    display: inline;
  }
  .delete-edit-button a {
    text-decoration: none;
    color: black;
    padding: 5px;
    border-radius: 10px;
  }
  .delete-button:hover,.edit-button:hover, .info-button:hover, .comment-button:hover  {
    color: gold;
  }
</style>
<!-- タグ -->
<style>
.board-container {
  width: 100%;
  min-width: 300px;
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  align-items: center;
  gap: 10px;
  padding: 10px;
  width: 100%;
  box-sizing: border-box;
}
.create-board i {
  color: #ff701e;
  transition: .7s;
}
.create-board i:hover {
  color: gold;
  letter-spacing: 0.2em;
}
.board-thumbnail-wrapper {
  display: flex;
  flex-direction: column;
  position: relative;
}
.board-thumbnail-container {
  position: absolute;
  top: -300px;
  overflow: hidden;
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  height: 300px;
  background: white;
  border: 2px solid #ddd;
  border-radius: 20px;
  display: none;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.5);
  z-index: 100;
}
.board-thumbnail {
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  height: 100%;
}
.board-thumbnail-text {
  text-align: center;
  padding: 5px;
}
.board-thumbnail img {
  max-width: 100%;
  max-height: 100%;
  object-fit: contain;
  margin: auto;
  display: block;
}

.board-container .board-content {
  border: 2px solid #ddd; 
  border-radius: 10px;
  padding: 10px 20px;
  text-align: center;
  width: 100%;
  box-sizing: border-box;
  font-size: 20px;
  font-weight: bold;
}
.board-container .board-content.red, .board-title-label.red, .comment-area-label.red, .title-board-content.red {
  background: #F4ADB0;
}
.board-container .board-content.green, .board-title-label.green, .comment-area-label.green, .title-board-content.green {
  background: #C9E2C7;
}
.board-container .board-content.blue, .board-title-label.blue, .comment-area-label.blue, .title-board-content.blue {
  background: #ABECF2;
}
.board-container .board-content.brown, .board-title-label.brown, .comment-area-label.brown, .title-board-content.brown {
  background: #EFDDAA;
}
.board-container .board-content.emerald, .board-title-label.emerald, .comment-area-label.emerald, .title-board-content.emerald {
  background: #E4EDAA;
}

.board-thumbnail-wrapper a { 
  color: black;
  text-decoration: none;
}
</style>

<style>

  <%# お気に入りボタン %>
  .like-button {
    position: absolute;
    left: 50%;
    bottom: 10px;
    transform: translate(-50%, 0);
    z-index: 100;
    background: #ff701e;
    border: 2px solid #ff701e;
    border-radius: 8px;
    color: #fff;
    font-weight: bold;
    text-align: center;
    background: linear-gradient(90deg, rgba(255,112,30,1) 0%, rgba(255,191,48,1) 50%, rgba(255,77,48,1) 100%);
    background-size: 200% 100%;
    border: none;
    transition: .4s cubic-bezier(0.37, 0, 0.63, 1);
    box-shadow: inset 0 -5px 8px rgba(0, 0, 0, 0.1);
   }
   .like-button:hover {
    background: #fff;
    letter-spacing: 0.15em;
    border: 2px solid #ff701e;
   }
   .like-button .unlike span, .like-button .like span {
    color: #fff;
    font-weight: bold;
   }
   .like-button:hover span {
    color: #ff701e;
   }
   .like-button a {
    padding: 5px 10px;
    font-size: 20px;
    text-decoration: none;
   }
   .like.not-login span {
    padding: 5px 10px;
    font-size: 20px;
    text-decoration: none;
    cursor: pointer;
   }
</style>

黒背景
<style>
.background-cover {
  position: fixed;
  width: 100%;
  height: 100%;
  background: #333;
  opacity: 0.3;
  z-index: 990;
  display: none;
}
</style>

<!-- 未ログイン時のボード機能表示タブ -->
<style>
  #login-tab {
    display: none;
  }
  .login-tab-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 500px;
    height: 300px;
    z-index: 999;
    background: #fff;
    border-radius: 10px;
  }
  .login-tab-title {
    font-size: 30px;
    color: #333;
    margin: 30px;
  }
  .login-tab-container span {
    color: #333;
  }
  .login-tab-container a {
    width: 40px;
  }
  .login-tab-container .line-icon, .login-tab-container .line-friend {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    height: 100%;
    margin: 20px;
  }
  .login-tab-container .line-icon img {
    width: 80px;
    height: 80px;
   }
  
</style>
<!-- チャット入力フォーム -->
<style>
  .comment-form-wrapper {
    position: fixed;
    top: 70%;
    left: 50%;
    transform: translate(-50%, -50%);
    height: 300px;
    border-radius: 20px;
    z-index: 999;
    display: none;
  }
  .chat-form textarea {
    position: absolute;
    height: 100%;
    padding-top: 60px;
    bottom: 0;
    resize: none;
    background-color: white;
    font-size: 30px;
    border-radius: 20px;
    box-shadow: inset 0 -5px 8px rgba(0, 0, 0, 0.1);
  }
  .chat-form input {
    width: 100%;
    opacity: 90%;
    height: 50px;
    font-size: 20px;
    border-radius: 20px 20px 0px 0px;
    background: #ff701e;
    border: 2px solid #ff701e;
    font-weight: bold;
    background: linear-gradient(90deg, rgba(255,112,30,1) 0%, rgba(255,191,48,1) 50%, rgba(255,77,48,1) 100%);
    background-size: 200% 100%;
    transition: .8s cubic-bezier(0.37, 0, 0.63, 1);
  }
  .chat-form input[type="submit"]:hover {
    background: #fff;
    color: #ff701e;
    letter-spacing: 0.15em;
    border: 2px solid #ff701e;
  }
  .chat-form input[type="submit"] {
    color: #fff;
    position: absolute;
    top: 0;
  }
  .chat-form input[type="submit"]:hover {
    opacity: 0.6;
  }
</style>